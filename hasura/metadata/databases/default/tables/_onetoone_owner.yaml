table:
  name: owner
  schema: _onetoone
object_relationships:
  - name: passport_info
    using:
      foreign_key_constraint_on:
        column: owner_id
        table:
          name: passport_info
          schema: _onetoone
array_relationships:
  - name: passport_infos
    using:
      foreign_key_constraint_on:
        column: owner_id
        table:
          name: passport_info
          schema: _onetoone
